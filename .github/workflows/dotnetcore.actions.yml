name: .NET Core

on:
  pull_request:
    branches: 
      master

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: testing secrets
      run: |
        export x="this is for testing secrets"
        echo "the variable x is -> $x"
        echo "the variable x is -> ${{ env.x }} "
    - name: testing secrets2
      run: |
        echo "the variable x is -> $x"
        echo "the variable x is -> ${{ env.x }} "
    - name: Set env
      run: echo "GITHUB_SHA_SHORT=$(echo $GITHUB_SHA | cut -c 1-6)" >> $GITHUB_ENV
    - name: Test
      run: echo $GITHUB_SHA_SHORT
        
